[
    {
        "id": "CP_RUNTIME_000501",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    int num = std::stoi(inputStr);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    parseInput(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    try {\n        int num = std::stoi(inputStr);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    parseInput(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000502",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    int num = std::stoi(userAge);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    convertString(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    try {\n        int num = std::stoi(userAge);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    convertString(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000503",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    int num = std::stoi(priceTag);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    processData(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    try {\n        int num = std::stoi(priceTag);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    processData(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000504",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    int num = std::stoi(amountInput);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    extractNumber(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    try {\n        int num = std::stoi(amountInput);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    extractNumber(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000505",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    int num = std::stoi(countVal);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    readCount(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    try {\n        int num = std::stoi(countVal);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    readCount(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000506",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    int num = std::stoi(scoreData);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    loadAge(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    try {\n        int num = std::stoi(scoreData);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    loadAge(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000507",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    int num = std::stoi(indexStr);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    fetchPrice(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    try {\n        int num = std::stoi(indexStr);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    fetchPrice(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000508",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    int num = std::stoi(limitInput);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    computeLimit(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    try {\n        int num = std::stoi(limitInput);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    computeLimit(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000509",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    int num = std::stoi(dataPoint);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    validateScore(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    try {\n        int num = std::stoi(dataPoint);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    validateScore(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000510",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid getNumber(const std::string& offsetStr) {\n    int num = std::stoi(offsetStr);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    getNumber(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid getNumber(const std::string& offsetStr) {\n    try {\n        int num = std::stoi(offsetStr);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    getNumber(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000511",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    int num = std::stoi(inputStr);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    parseInput(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    try {\n        int num = std::stoi(inputStr);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    parseInput(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000512",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    int num = std::stoi(userAge);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    convertString(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    try {\n        int num = std::stoi(userAge);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    convertString(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000513",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    int num = std::stoi(priceTag);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    processData(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    try {\n        int num = std::stoi(priceTag);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    processData(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000514",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    int num = std::stoi(amountInput);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    extractNumber(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    try {\n        int num = std::stoi(amountInput);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    extractNumber(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000515",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    int num = std::stoi(countVal);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    readCount(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    try {\n        int num = std::stoi(countVal);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    readCount(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000516",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    int num = std::stoi(scoreData);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    loadAge(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    try {\n        int num = std::stoi(scoreData);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    loadAge(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000517",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    int num = std::stoi(indexStr);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    fetchPrice(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    try {\n        int num = std::stoi(indexStr);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    fetchPrice(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000518",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    int num = std::stoi(limitInput);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    computeLimit(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    try {\n        int num = std::stoi(limitInput);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    computeLimit(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000519",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    int num = std::stoi(dataPoint);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    validateScore(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    try {\n        int num = std::stoi(dataPoint);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    validateScore(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000520",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid getNumber(const std::string& offsetStr) {\n    int num = std::stoi(offsetStr);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    getNumber(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid getNumber(const std::string& offsetStr) {\n    try {\n        int num = std::stoi(offsetStr);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    getNumber(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000521",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    int num = std::stoi(inputStr);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    parseInput(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    try {\n        int num = std::stoi(inputStr);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    parseInput(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000522",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    int num = std::stoi(userAge);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    convertString(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    try {\n        int num = std::stoi(userAge);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    convertString(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000523",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    int num = std::stoi(priceTag);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    processData(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    try {\n        int num = std::stoi(priceTag);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    processData(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000524",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    int num = std::stoi(amountInput);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    extractNumber(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    try {\n        int num = std::stoi(amountInput);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    extractNumber(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000525",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument runtime exception caused by parsing a non-numeric string.",
        "input": {
            "title": "std::stoi invalid_argument",
            "code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    int num = std::stoi(countVal);\n    std::cout << num * 2 << std::endl;\n}\n\nint main() {\n    readCount(\"abc10\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stoi\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::stoi` function throws a `std::invalid_argument` exception if no conversion could be performed because the string does not start with a valid numeric character.",
            "beginner": "You tried to convert text into a number, but the text started with letters. The computer doesn't know how to turn 'abc' into a math number.",
            "analogy": "It's like putting a plastic toy apple into a juicer and expecting apple juice.",
            "solution": "Wrap the conversion in a `try-catch` block to handle `std::invalid_argument` and `std::out_of_range` exceptions gracefully.",
            "prevention": "Validate user input using string matching or regular expressions before attempting to parse it into an integer.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    try {\n        int num = std::stoi(countVal);\n        std::cout << num * 2 << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"Invalid input: not a number\" << std::endl;\n    }\n}\n\nint main() {\n    readCount(\"abc10\");\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ reference documentation",
            "language_version": "C++11",
            "tags": [
                "std::stoi",
                "exception",
                "parsing"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000526",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    double val = std::stod(priceTag);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    processData(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    if (priceTag.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(priceTag);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    processData(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000527",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    double val = std::stod(amountInput);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    extractNumber(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    if (amountInput.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(amountInput);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    extractNumber(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000528",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    double val = std::stod(countVal);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    readCount(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    if (countVal.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(countVal);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    readCount(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000529",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    double val = std::stod(scoreData);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    loadAge(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    if (scoreData.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(scoreData);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    loadAge(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000530",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    double val = std::stod(indexStr);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    fetchPrice(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    if (indexStr.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(indexStr);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    fetchPrice(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000531",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    double val = std::stod(limitInput);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    computeLimit(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    if (limitInput.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(limitInput);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    computeLimit(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000532",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    double val = std::stod(dataPoint);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    validateScore(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    if (dataPoint.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(dataPoint);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    validateScore(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000533",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid getNumber(const std::string& offsetStr) {\n    double val = std::stod(offsetStr);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    getNumber(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid getNumber(const std::string& offsetStr) {\n    if (offsetStr.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(offsetStr);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    getNumber(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000534",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    double val = std::stod(inputStr);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    parseInput(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    if (inputStr.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(inputStr);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    parseInput(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000535",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    double val = std::stod(userAge);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    convertString(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    if (userAge.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(userAge);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    convertString(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000536",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    double val = std::stod(priceTag);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    processData(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    if (priceTag.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(priceTag);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    processData(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000537",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    double val = std::stod(amountInput);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    extractNumber(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    if (amountInput.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(amountInput);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    extractNumber(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000538",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    double val = std::stod(countVal);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    readCount(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    if (countVal.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(countVal);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    readCount(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000539",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    double val = std::stod(scoreData);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    loadAge(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    if (scoreData.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(scoreData);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    loadAge(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000540",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    double val = std::stod(indexStr);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    fetchPrice(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    if (indexStr.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(indexStr);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    fetchPrice(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000541",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    double val = std::stod(limitInput);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    computeLimit(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    if (limitInput.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(limitInput);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    computeLimit(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000542",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    double val = std::stod(dataPoint);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    validateScore(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    if (dataPoint.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(dataPoint);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    validateScore(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000543",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid getNumber(const std::string& offsetStr) {\n    double val = std::stod(offsetStr);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    getNumber(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid getNumber(const std::string& offsetStr) {\n    if (offsetStr.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(offsetStr);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    getNumber(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000544",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    double val = std::stod(inputStr);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    parseInput(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    if (inputStr.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(inputStr);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    parseInput(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000545",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    double val = std::stod(userAge);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    convertString(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    if (userAge.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(userAge);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    convertString(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000546",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    double val = std::stod(priceTag);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    processData(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    if (priceTag.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(priceTag);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    processData(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000547",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    double val = std::stod(amountInput);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    extractNumber(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    if (amountInput.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(amountInput);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    extractNumber(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000548",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    double val = std::stod(countVal);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    readCount(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    if (countVal.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(countVal);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    readCount(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000549",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    double val = std::stod(scoreData);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    loadAge(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    if (scoreData.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(scoreData);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    loadAge(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000550",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing an empty string to std::stod.",
        "input": {
            "title": "std::stod empty string",
            "code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    double val = std::stod(indexStr);\n    std::cout << val << std::endl;\n}\n\nint main() {\n    fetchPrice(\"\");\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  stod\nAborted (core dumped)"
        },
        "output": {
            "technical": "Passing an empty string to `std::stod` (or `std::stoi`, `std::stof`) results in a `std::invalid_argument` exception because there are no characters to process.",
            "beginner": "You asked the computer to read a number from an empty piece of text. It panicked because there was nothing to read.",
            "analogy": "It's like asking a translator to translate a blank sheet of paper.",
            "solution": "Check if the string is `.empty()` before passing it to parsing functions, and use `try-catch` blocks.",
            "prevention": "Ensure variables representing input are always initialized properly and check for empty input from users or files.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    if (indexStr.empty()) {\n        std::cerr << \"Error: empty string provided\" << std::endl;\n        return;\n    }\n    try {\n        double val = std::stod(indexStr);\n        std::cout << val << std::endl;\n    } catch (const std::exception& e) {\n        std::cerr << \"Parsing error\" << std::endl;\n    }\n}\n\nint main() {\n    fetchPrice(\"\");\n    return 0;\n}",
            "complexity": {
                "before": "O(1)",
                "after": "O(1)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::stod",
                "empty-string",
                "exception"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000551",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    std::cout << countVal << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    readCount(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    std::cout << countVal << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        readCount(c_str);\n    } else {\n        readCount(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000552",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    std::cout << scoreData << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    loadAge(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    std::cout << scoreData << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        loadAge(c_str);\n    } else {\n        loadAge(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000553",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    std::cout << indexStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    fetchPrice(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    std::cout << indexStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        fetchPrice(c_str);\n    } else {\n        fetchPrice(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000554",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    std::cout << limitInput << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    computeLimit(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    std::cout << limitInput << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        computeLimit(c_str);\n    } else {\n        computeLimit(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000555",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    std::cout << dataPoint << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    validateScore(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    std::cout << dataPoint << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        validateScore(c_str);\n    } else {\n        validateScore(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000556",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid getNumber(const std::string& offsetStr) {\n    std::cout << offsetStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    getNumber(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid getNumber(const std::string& offsetStr) {\n    std::cout << offsetStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        getNumber(c_str);\n    } else {\n        getNumber(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000557",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    std::cout << inputStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    parseInput(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    std::cout << inputStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        parseInput(c_str);\n    } else {\n        parseInput(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000558",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    std::cout << userAge << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    convertString(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    std::cout << userAge << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        convertString(c_str);\n    } else {\n        convertString(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000559",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    std::cout << priceTag << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    processData(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    std::cout << priceTag << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        processData(c_str);\n    } else {\n        processData(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000560",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    std::cout << amountInput << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    extractNumber(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    std::cout << amountInput << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        extractNumber(c_str);\n    } else {\n        extractNumber(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000561",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    std::cout << countVal << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    readCount(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    std::cout << countVal << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        readCount(c_str);\n    } else {\n        readCount(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000562",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    std::cout << scoreData << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    loadAge(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    std::cout << scoreData << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        loadAge(c_str);\n    } else {\n        loadAge(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000563",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    std::cout << indexStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    fetchPrice(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    std::cout << indexStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        fetchPrice(c_str);\n    } else {\n        fetchPrice(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000564",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    std::cout << limitInput << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    computeLimit(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    std::cout << limitInput << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        computeLimit(c_str);\n    } else {\n        computeLimit(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000565",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    std::cout << dataPoint << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    validateScore(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    std::cout << dataPoint << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        validateScore(c_str);\n    } else {\n        validateScore(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000566",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid getNumber(const std::string& offsetStr) {\n    std::cout << offsetStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    getNumber(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid getNumber(const std::string& offsetStr) {\n    std::cout << offsetStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        getNumber(c_str);\n    } else {\n        getNumber(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000567",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    std::cout << inputStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    parseInput(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid parseInput(const std::string& inputStr) {\n    std::cout << inputStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        parseInput(c_str);\n    } else {\n        parseInput(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000568",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    std::cout << userAge << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    convertString(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid convertString(const std::string& userAge) {\n    std::cout << userAge << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        convertString(c_str);\n    } else {\n        convertString(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000569",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    std::cout << priceTag << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    processData(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid processData(const std::string& priceTag) {\n    std::cout << priceTag << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        processData(c_str);\n    } else {\n        processData(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000570",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    std::cout << amountInput << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    extractNumber(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid extractNumber(const std::string& amountInput) {\n    std::cout << amountInput << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        extractNumber(c_str);\n    } else {\n        extractNumber(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000571",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    std::cout << countVal << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    readCount(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid readCount(const std::string& countVal) {\n    std::cout << countVal << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        readCount(c_str);\n    } else {\n        readCount(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000572",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    std::cout << scoreData << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    loadAge(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid loadAge(const std::string& scoreData) {\n    std::cout << scoreData << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        loadAge(c_str);\n    } else {\n        loadAge(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000573",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    std::cout << indexStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    fetchPrice(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid fetchPrice(const std::string& indexStr) {\n    std::cout << indexStr << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        fetchPrice(c_str);\n    } else {\n        fetchPrice(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000574",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    std::cout << limitInput << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    computeLimit(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid computeLimit(const std::string& limitInput) {\n    std::cout << limitInput << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        computeLimit(c_str);\n    } else {\n        computeLimit(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000575",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::logic_error exception caused by implicitly constructing a std::string from a null pointer.",
        "input": {
            "title": "Constructing std::string from nullptr",
            "code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    std::cout << dataPoint << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    validateScore(c_str);\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::logic_error'\n  what():  basic_string::_M_construct null not valid\nAborted (core dumped)"
        },
        "output": {
            "technical": "Constructing a `std::string` from a `nullptr` violates the preconditions of the string constructor, resulting in a `std::logic_error`.",
            "beginner": "You tried to create text from a pointer that points to absolutely nothing. C++ doesn't allow creating a string out of nothing.",
            "analogy": "It's like trying to build a house, but instead of giving the builder a blueprint, you give them empty air.",
            "solution": "Check if the C-style string pointer is `nullptr` before passing it to a function that expects a `std::string`.",
            "prevention": "Be extremely careful when interfacing C++ code with older C APIs that might return `nullptr`.",
            "optimized_code": "#include <iostream>\n#include <string>\n\nvoid validateScore(const std::string& dataPoint) {\n    std::cout << dataPoint << std::endl;\n}\n\nint main() {\n    const char* c_str = nullptr;\n    if (c_str != nullptr) {\n        validateScore(c_str);\n    } else {\n        validateScore(\"default_string\");\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by common C++ logic errors",
            "language_version": "C++11",
            "tags": [
                "std::string",
                "nullptr",
                "logic-error"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000576",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000577",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000578",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000579",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000580",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000581",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000582",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000583",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000584",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000585",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000586",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000587",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000588",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000589",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000590",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000591",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000592",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000593",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000594",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000595",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000596",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000597",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000598",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000599",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    },
    {
        "id": "CP_RUNTIME_000600",
        "language": "cpp",
        "category": "runtime_error",
        "instruction": "Fix the std::invalid_argument exception caused by passing non-binary characters to std::bitset.",
        "input": {
            "title": "std::bitset invalid_argument",
            "code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    std::bitset<8> b(bits);\n    std::cout << b << std::endl;\n    return 0;\n}",
            "error_message": "terminate called after throwing an instance of 'std::invalid_argument'\n  what():  bitset::_M_copy_from_ptr\nAborted (core dumped)"
        },
        "output": {
            "technical": "The `std::bitset` string constructor throws `std::invalid_argument` if any character in the provided string is not '0' or '1'.",
            "beginner": "You gave a piece of text to a system that only understands '0's and '1's, but your text had a '2' in it.",
            "analogy": "It's like giving a multiple-choice answer sheet (A, B, C, D) but bubbling in 'E'.",
            "solution": "Wrap the `std::bitset` initialization in a `try-catch` block, or validate the string first to ensure it only contains '0' and '1'.",
            "prevention": "When dealing with strict binary conversions, sanitize the input beforehand.",
            "optimized_code": "#include <iostream>\n#include <bitset>\n#include <string>\n\nint main() {\n    std::string bits = \"10102010\";\n    try {\n        std::bitset<8> b(bits);\n        std::cout << b << std::endl;\n    } catch (const std::invalid_argument& e) {\n        std::cerr << \"String contains non-binary characters\" << std::endl;\n    }\n    return 0;\n}",
            "complexity": {
                "before": "O(N)",
                "after": "O(N)"
            }
        },
        "metadata": {
            "difficulty": "medium",
            "source": "Inspired by C++ standard library behavior",
            "language_version": "C++11",
            "tags": [
                "std::bitset",
                "exception",
                "invalid-argument"
            ]
        }
    }
]